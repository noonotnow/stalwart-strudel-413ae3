[functions]
  directory = "netlify/functions"
  external_node_modules = ["sharp"]

[build]
  command = "cd phase0 && npm install && npm run build"
  publish = "phase0/dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

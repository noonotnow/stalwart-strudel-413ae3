[functions]
  directory = "netlify/functions"

[build]
  command = "cd phase0 && npm install && npm run build"
  publish = "phase0/dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
